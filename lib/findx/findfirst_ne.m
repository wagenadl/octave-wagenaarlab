function idx=findfirst_ne(x,y)
% idx=FINDFIRST_NE(x,y) finds the index of the first occurrance of a value
% other than the scalar Y in the vector X.
% If not found, return 0.
% Note: Both X and Y must be real.