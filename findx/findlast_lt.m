function idx=findlast_lt(x,y)
% idx=FINDLAST_LT(x,y) finds the index of the last element in the vector
% X that is less than to the scalar Y.
% If not found, return 0.
% Note: Both X and Y must be real.
