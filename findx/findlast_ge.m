function idx=findlast_ge(x,y)
% idx=FINDLAST_GE(x,y) finds the index of the last element in the vector
% X that is larger than or equal to the scalar Y.
% If not found, return 0.
% Note: Both X and Y must be real.
