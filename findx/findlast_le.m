function idx=findlast_le(x,y)
% idx=FINDLAST_LE(x,y) finds the index of the last element in the vector
% X that is less than or equal to the scalar Y.
% If not found, return 0.
% Note: Both X and Y must be real.
